#!/bin/bash

# Скрипт, проверяющий тип файла.

if [[ -d $1 ]]; then  # если файл - каталог:
    ls $1  # то вывести его содержимое.

else  # иначе:
    stat --format=%F $1  # вывести сообщение о типе файла.
fi
